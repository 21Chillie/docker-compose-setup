How to use:

Clone the project

```bash
git clone https://github.com/21Chillie/docker-compose-setup.git
```

then cd to config folder

```bash
cd <project>
```

after that, run the docker compose

```bash
docker-compose up -d
```
